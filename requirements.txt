Flask>=2.2.2 
Flask-Cors==3.0.8 # A Flash extension for handling Cross Origin Resource Sharing (CORS), making cross-origin AJAX possible 
matplotlib==3.1.2
numpy==1.18.1
pandas==0.25.3
scikit-learn==0.22.1
kneed==0.5.1
xgboost==1.0.2
Flask-MonitoringDashboard==3.0.6 # Flask app monitoring dashboard 
Jinja2==2.11.0 # Flask is based on the Jinja2 template engine
Werkzeug==0.16.1 # Flask is based on the Werkzeug WSGI toolkit
itsdangerous==1.1.0 # Various helpers to pass data to untrusted environments and to get it back sage and sound.
certifi==2019.11.28 # Certifi is a carefully curated collection of Root Certificates for validating the trustworthiness
MarkupSafe==1.1.1 # MarkupSafe implements a text object that escapes characters so it is sage to use in HTML and XML